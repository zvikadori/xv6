#include "types.h"
#include "stat.h"
#include "user.h"

#ifndef sighandler_t
typedef void (*sighandler_t)(void);
#endif

int main (void){
	//printf("\n%d\n\n\n\n\n", 512);
	//sighandler_t firstHandler = (sighandler_t) 5;
	
	//signal(1, firstHandler);
	
	//sigsend(getpid(), 3);
	//sigsend(getpid(), 6);
	
	//sigsend(getpid(), 0);
	//sigsend(getpid(), 5);
	//sigsend(getpid(), 10);
	//sigsend(getpid(), 10);
	exit();
}
